# Database
MONGODB_URI=mongodb://localhost:27017/medpage

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Domain Configuration
NEXT_PUBLIC_ROOT_DOMAIN=localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Paddle Configuration
NEXT_PUBLIC_PADDLE_ENVIRONMENT=sandbox
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=your-paddle-client-token
PADDLE_API_KEY=your-paddle-api-key
# Secret de SIGNATURE du webhook ("Signing key" de la notification
# destination, Developer tools > Notifications). Pas la clé API.
PADDLE_WEBHOOK_SECRET=your-paddle-webhook-secret
# Optionnel : secret d'une destination webhook antérieure encore active.
# Accepté en plus de PADDLE_WEBHOOK_SECRET pour éviter des retries inutiles
# pendant une rotation.
PADDLE_WEBHOOK_SECRET_2=your-legacy-paddle-webhook-secret
PADDLE_PRICE_ID=your-paddle-price-id

# Google Maps (for location maps)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Cloudinary (for image uploads)
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=your-upload-preset

# Resend (for emails)
RESEND_API_KEY=your-resend-api-key
RESEND_FROM_EMAIL=MedPage <bonjour@medpage.fr>

# Cron Jobs (for trial reminder emails)
CRON_SECRET=your-cron-secret-here